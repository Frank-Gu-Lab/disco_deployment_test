name: discoprocess
channels:
  - conda-forge
dependencies:
  - matplotlib
  - seaborn
  - numpy
  - pandas
  - pytest
  - pytest-mock
  - pytest-cov
  - openpyxl
  - scikit-learn # sklearn

# +SciencePlots (via pip install)